# Web UI 安全侦察报告

> 目标路径：`{目标代码路径}`
> 分析模式：{full|quick}
> 分析时间：{YYYY-MM-DD}

---

## 1. 功能模块地图

| 模块 | 路由路径 | 访问权限 | 操作类型 |
|------|---------|---------|---------|
| {模块名} | {路径} | {公开/登录/admin} | {业务/管理/文件/配置} |
| ... | ... | ... | ... |

---

## 2. 界面输入元素清单

### 2.1 {页面路径/名称}
用户通过{文本框/密码框}输入{字段说明}（{必填}）。
通过{下拉框}选择{字段说明}，选项由 API 加载 [E{n}]。
通过{复选框}选择{字段说明}。
通过{文件上传}上传{字段说明}（接受 {文件类型}）。
点击「{按钮文字}」按钮/链接执行{操作说明}。

---

## 3. API 端点与参数 → 输入元素映射

> `[E{n}]` 编号供其他章节引用。

```
E1: {方法} {端点}
发送{功能说明}请求。{Body/Query/Path} 包含{参数列表}（值来自{界面元素}）。
{需/无需}鉴权。
```

---

## 4. 业务功能-API 映射

```
{业务功能} ──→ [E1] ──→ 输入({参数}) → {后端操作}
  │
  {子功能} ──→ [E{n}]
```

---

## 5. 功能与配置汇总

### 5.1 功能操作清单

| # | 操作 | 所在页面 | 对应 API |
|---|------|---------|---------|
| 1 | {操作描述} | {页面} | [E{n}] |
| ... | ... | ... | ... |

### 5.2 输入校验规则

| 输入位置 | 校验规则 | 约束条件 |
|---------|---------|---------|
| {页面.字段} | {required/min/max/pattern/type} | {如"至少8位含大小写"} |

### 5.3 敏感信息输入

| 输入位置 | 类型 |
|---------|------|
| {字段} | {密码/凭据/PII} |

### 5.4 URL/地址配置

| 配置项 | 当前值 | 说明 |
|-------|-------|------|
| {字段} | {值} | {该配置的用途} |

### 5.5 协议配置

| 配置项 | 当前值 | 可选项 |
|-------|-------|-------|
| {字段} | {值} | {可选值列表} |

### 5.6 动态执行能力

| # | 位置 | 描述 |
|---|------|------|
| 1 | {组件/页面} | {支持动态代码/公式/脚本/规则} |

> 仅当存在动态执行能力时列出此小节。不存在则跳过。

---

## 6. 错误处理与输入约束

### 前端校验规则

| 输入位置 | 校验规则 | 约束条件 |
|---------|---------|---------|
| {页面.字段} | {required/min/max/pattern/type} | {如"至少8位含大小写"} |

### 错误信息

| 操作场景 | 触发条件 | 错误信息 | 反馈方式 |
|---------|---------|---------|---------|
| {操作} | {条件} | "{错误文案}" | {弹窗/行内/toast} |

### 认证错误信息

- 登录失败提示：`"{具体文案}"` — {通用/区分用户是否存在}

### API 错误处理

- {端点} catch 处理：{直接展示后端错误 / 统一错误}
